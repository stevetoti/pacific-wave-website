export default async function Page({searchParams}:{searchParams:Promise<{token?:string}>}){const {token}=await searchParams;return <div style={{maxWidth:600,margin:'70px auto',padding:32}}><h1>Training email preferences</h1><p>Stop receiving training campaigns and course follow-up emails. Your account, course access and essential account emails remain available.</p><form action={`/api/training-unsubscribe?token=${encodeURIComponent(token||'')}`} method="post"><button style={{background:'#233c6f',color:'white',padding:'14px 22px',borderRadius:8,marginTop:20}}>Unsubscribe from campaign emails</button></form></div>;}

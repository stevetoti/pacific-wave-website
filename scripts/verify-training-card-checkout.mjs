import Stripe from 'stripe';
const stripe=new Stripe(process.env.STRIPE_SECRET_KEY,{timeout:15000,maxNetworkRetries:1});let session;
try{session=await stripe.checkout.sessions.create({mode:'payment',payment_method_types:['card'],metadata:{application:'pwd_training_preflight'},line_items:[{quantity:1,price_data:{currency:'vuv',unit_amount:35000,product_data:{name:'Pacific Wave Digital training setup validation — do not pay'}}}],success_url:'https://pacificwavedigital.com/training-center/dashboard',cancel_url:'https://pacificwavedigital.com/training-center'},{idempotencyKey:'pwd-training-unpaid-validation-20260915'});console.log(JSON.stringify({checkout_created:!!session.id,currency:session.currency,amount:session.amount_total,payment_status:session.payment_status,live:session.livemode}));}finally{if(session?.status==='open'){await stripe.checkout.sessions.expire(session.id);console.log('Unpaid checkout expired. No card details entered and no charge made.');}}
